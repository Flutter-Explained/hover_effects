name: hover_effect
description: A new Flutter project.
publish_to: "none"
version: 0.1.0

environment:
  sdk: ">=3.0.6 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  flutter_octicons: ^0.42.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/isfahan.jpg
    - assets/avatar.jpg
  fonts:
    - family: MonaSans
      fonts:
        - asset: fonts/MonaSans-Regular.ttf
